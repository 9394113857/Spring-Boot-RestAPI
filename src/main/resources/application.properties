# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/spring_restapi
spring.datasource.username=root
spring.datasource.password=raghu
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update

# Enable CORS (Cross-Origin Resource Sharing)
# Allow requests from any origin, you can modify this as needed
# This is useful for development purposes; in production, you should restrict origins.
spring.mvc.cors.allowed-origins=*

# Server port
server.port=8080

# Additional MySQL Configurations (if needed)
# spring.datasource.hikari.connectionTimeout=20000
# spring.datasource.hikari.maximumPoolSize=5
# spring.datasource.hikari.minimumIdle=1
# spring.datasource.hikari.idleTimeout=30000
# spring.datasource.hikari.poolName=myHikariPool

# Logging SQL Statements (optional, for debugging purposes)
# spring.jpa.show-sql=true

# Use Unicode and UTF-8 encoding
# spring.datasource.url=jdbc:mysql://localhost:3306/spring_restapi?useUnicode=true&characterEncoding=UTF-8

# If you have additional properties or configurations, add them here
