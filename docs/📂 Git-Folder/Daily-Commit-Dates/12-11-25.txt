git commit -m "Feature: Spring Boot + Angular CRUD Project Setup and Integration

1. **Initialized Spring Boot backend**:
   - Configured MySQL datasource and JPA properties.
   - Created User entity and JPA repository for database interaction.
   - Implemented CRUD operations in UserController with REST endpoints.

2. **Integrated Angular frontend**:
   - Connected Angular services with backend REST APIs.
   - Implemented components for listing, creating, updating, and deleting users.
   - Ensured live data updates with full roundtrip from UI → backend → database.

3. **Tested end-to-end functionality**:
   - Verified CRUD operations via Angular UI and observed Hibernate SQL logs.
   - Confirmed MySQL database tables updated correctly on operations.

4. **Configured CORS and application properties**:
   - Enabled cross-origin requests for Angular frontend.
   - Set server port and MySQL credentials in application.properties.

5. **Prepared for further development and enhancements**:
   - Project structure ready for adding validations, exception handling, and security features.
   - Angular components modularized for future extensions.

Date: $(Get-Date -Format 'yyyy-MM-dd hh:mm tt')"
